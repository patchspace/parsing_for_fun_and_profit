require 'treetop'
Treetop.load(File.dirname(__FILE__) + '/list')

module ParsingForFunAndProfit
  module List
    module List
      # If we want any named modules for Treetop Syntax nodes,
      # we can put them here. This example doesn't really need
      # them though, as we define all methods in anonymous
      # modules directly in the grammar.
    end
  end
end